version: "3.4"
services:
  torr-server-dlna:
    network_mode: host
    container_name: torr-server-dlna
    build:
      context: ../
      dockerfile: dockerized/Dockerfile
    restart: always
    # command: bash
    # stdin_open: true
    # tty: true
